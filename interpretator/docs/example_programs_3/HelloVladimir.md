```
НАЧАЛО
    CТРОКА имя;
    ВНЕМЛИ(имя);
    ЧИСЛО лета : ДРОБЬ;
    ВНЕМЛИ(лета);
    
    ФУНКЦИЯ Приветствие(имя : СТРОКА, лета : ДРОБЬ)
    НАЧАЛО
        ЕСЛИ (лета >= 18) 
        СТАЛОБЫТЬ
            НАЧАЛО
                МОЛВИ("Здарова, ", имя);
            ИСХОД
        ИНО
            НАЧАЛО
                МОЛВИ("Мир тебе, ", имя");
            ИСХОД
        ИСХОД
    ИСХОД

    Приветствие(имя, лета);
ИСХОД
```