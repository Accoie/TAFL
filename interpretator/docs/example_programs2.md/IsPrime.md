```
НАЧАЛО
    ФУНКЦИЯ простоЛи(н: ДРОБЬ) : ДРОБЬ
    НАЧАЛО
        ЕСЛИ (н <= 1) 
        СТАЛОБЫТЬ
            ДАРОВАТЬ ЛОЖЬ;
        ИСХОД
        
        ЕСЛИ (н == 2) 
        СТАЛОБЫТЬ
            ДАРОВАТЬ ИСТИНА;
        ИСХОД
        
        ЕСЛИ (н % 2 == 0) 
        СТАЛОБЫТЬ
            ДАРОВАТЬ ЛОЖЬ;
        ИСХОД
        
        ЧИСЛО делитель : ДРОБЬ = 3;
        ПОКУДА (делитель * делитель <= н) 
        ТВОРИ
            ЕСЛИ (н % делитель == 0) 
            СТАЛОБЫТЬ
                ДАРОВАТЬ ЛОЖЬ;
            ИСХОД
            делитель = делитель + 2;
        ИСХОД
        
        ДАРОВАТЬ ИСТИНА;
    ИСХОД
    
    ЧИСЛО n : ДРОБЬ;
    МОЛВИ("Введите число для проверки на простоту: ");
    ВНЕМЛИ(n);
    
    ДРОБЬ результат1 : ДРОБЬ = простоЛи(n);
    
    ЕСЛИ (результат1 == 1) 
    СТАЛОБЫТЬ
        МОЛВИ("Число ", n, " простое (метод 1)");
    ИНО
        МОЛВИ("Число ", n, " составное (метод 1)");
    ИСХОД
ИСХОД
```